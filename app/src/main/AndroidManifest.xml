<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.punissementmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="punishement"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GestionPunissement"></activity>
        <activity
            android:theme="@style/AppTheme"
            android:label="Calendrier"
            android:name=".CalendarActivity" />
        <activity
            android:theme="@style/AppTheme"
            android:label="Punissement"
            android:name=".PunissementActivity" />
        <activity
            android:theme="@style/AppTheme"
            android:label="Stagiaire"
            android:name=".StagiaireActivity"></activity>
        <activity

            android:name=".ConnexionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity"></activity>
    </application>

</manifest>